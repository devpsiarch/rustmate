/* 
* We seach for moves in this module , well do all sort of ways to do so ...
* 1- random move generator <baby step>
* */

use crate::movegen::movecode::Move;

pub fn search_move(depth:u32) -> Move{
    depth
}
